import styled from 'styled-components';

export const Alert = styled.div`
  width: 31.25rem;
  display: flex;
  font-weight: bolder;
  justify-content: center;
  margin-bottom: 1.25rem;
  font-size: 1.313rem;
`;

export const ExclamationMark = styled.i`
  color: black;
  font-size: 2.188rem;
  animation: BlinkingExcMark 1s infinite;
  
`;
